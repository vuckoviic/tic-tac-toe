Gameboard = (function() {
    let board = [];

    drawToken = function(row, column, player) {
        // method takes row, column and player. 
        //From player object it finds player's token
        //and calculates positions relying on row and column
    }

    getBoard = function() {
        return board;
    }

    return {
        drawToken,
        getBoard
    }
})()